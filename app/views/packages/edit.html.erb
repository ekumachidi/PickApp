<div class="page-header">
  <%= link_to user_package_path(current_user,@package), class: 'btn btn-default' do %>
    <span class="glyphicon glyphicon-list-alt"></span>
    Back
  <% end %>
  <%= link_to edit_user_package_path(current_user, @package), class: 'btn btn-primary' do %>
    <span class="glyphicon glyphicon-info-sign"></span>
    Show
  <% end %>
  <h1>Editing package</h1>
</div>

<%= render 'form' %>
