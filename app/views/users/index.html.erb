<table>
	<tr>
		<th>name</th>
		<th>role</th>
		<th>location</th>
		<th>Action</th>
	</tr>



<tr><% @normal.each do |user|%>
  <td><%= user.profile.name%> </td>
  <td><%= user.role.name%></td>
  <td><%= user.profile.address%></td> 
  <td><%= link_to edit_user_path(user), class: 'btn btn-default' do %>
    <span class="glyphicon glyphicon-list-alt"></span>
    Change role</td>
    </tr>
  <% end %>
<%end%>
</table>

